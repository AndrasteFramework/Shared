name: Build Repository
on: [push]
 
jobs:
  build:
    runs-on: windows-latest
 
    steps:
    - name: Checkout
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    # Add  MSBuild to the PATH: https://github.com/microsoft/setup-msbuild
    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v1.1

    - name: Restore NuGet Packages
      run: nuget restore Andraste.Shared.csproj -PackagesDirectory packages
 
    - name: Build project
      run: msbuild Andraste.Shared.csproj /p:Configuration=Release /p:RestorePackagesPath=packages /t:restore
 
    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: HostDLL
        path: |
          bin\Release\netstandard2.0\Andraste.Shared.dll
          bin\Release\netstandard2.0\Andraste.Shared.pdb
